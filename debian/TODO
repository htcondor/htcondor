* Do license checks
* debconf config doesn't work on first run when doing an install from scratch
* debconf templates should mention format things: comma-separated list, ...
* debconf: think about switching away from combinatorial setup to a few
  hand-polished config scenarios
* Allow for configurable UID/GID
* Security: ALLOW_ADVERTISE_SCHEDD
* Incompatible with GSOAP 2.8.*
* Something is wrong with
  BIND_ALL_INTERFACES = FALSE
  NETWORK_INTERFACE =  127.0.0.1
  The collector still listens on the external interface (only)
  (emailed to condor-user on Sep 30 2011)

