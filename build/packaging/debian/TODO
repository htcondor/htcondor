* Allow for configurable UID/GID
* Security: ALLOW_ADVERTISE_SCHEDD
* Have a test suite that can run at package build time
* htcondor: package-installs-into-obsolete-dir etc/bash_completion.d/ :
  ^etc/bash_completion.d/ -> usr/share/bash-completion
  This is configured in this way by condor upstream
