PRINTABLE = POSTSCRIPT CAT

all: obj_distrib

RELEASE_DIR = ../src/release_dir
DOC_DIR = $(RELEASE_DIR)/doc

$(DOC_DIR):
	- mkdir $(DOC_DIR)

obj_distrib: $(DOC_DIR)
	cp README $(RELEASE_DIR)
	cp -r release_notes $(DOC_DIR)
	rm -r $(DOC_DIR)/release_notes/CVS
	cp -r $(PRINTABLE) $(DOC_DIR)

