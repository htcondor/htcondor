\begin{ManPage}{\label{man-condor-set-shutdown}\Condor{off}}{1}
{Shutdown Condor daemons}
\Synopsis \SynProg{\Condor{set\_shutdown}}
\ToolArgsBase

\SynProg{\Condor{set\_shutdown}}
\oOptnm{-exec}
\ToolDebugOption
\ToolArgsLocate
\ToolArgsAffect

\SynProg{\Condor{set\_shutdown}}
\oOptnm{-exec}
\ToolDebugOption
\ToolWhere
\ToolArgsAffect

\index{Condor commands!condor\_set\_shutdown}
\index{condor\_off command}

\Description 

\Condor{set\_shutdown} sets a program (typically a script) to execute
when the master shuts down.  The \OptArg{-exec}{$<$name$>$} paramter
is required, and specifies the program to run.  This name must match a
\MacroNI{MASTER\_SHUTDOWN\_$<$name$>$} in the \Condor{master}'s
configuration, otherwise the master will log an error and ignore the
request.

For security purposes (authentication and authorization),
this command requires an administrator's level of access.
See
section~\ref{sec:Config-Security} on page~\pageref{sec:Config-Security}
for further explanation.

\begin{Options}
	\ToolArgsBaseDesc
	\OptItem{\Opt{-exec $<$name$>$}}{Specify the name of the
	program to execute}
	\ToolDebugDesc
	\ToolArgsLocateDesc
	\ToolArgsAffectDesc
\end{Options}

\ExitStatus
\Condor{set\_shutdown} will exit with a status value of 0 (zero) upon
success, and it will exit with the value 1 (one) upon failure.


\Examples
To have all master's run ``/bin/reboot'' on shutdown, configure the
master with:
\begin{verbatim}
MASTER_SHUTDOWN_REBOOT = /bin/reboot
\end{verbatim}
and then run
\begin{verbatim}
% condor_set_shutdown -exec reboot -all
% condor_off -graceful -all
\end{verbatim}

\end{ManPage}
