universe = vanilla
executable = /bin/sleep
arguments = 20
output = autoscale.out
error = autoscale.err
log = autoscale.log
periodic_remove = (CurrentTime - QDate) > (24 * 60 * 60)
should_transfer_files   = YES
when_to_transfer_output = ON_EXIT_OR_EVICT
Notification = NEVER
queue 20

