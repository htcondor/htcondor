#! /usr/bin/env perl
use CondorTest;

$testname = 'Condor PERIODIC_HOLD policy test - java U';

my $nostatus = CondorTest::verbose_system("job_core_perhold-false_java.pl");
my $willstatus = CondorTest::verbose_system("job_core_perhold-true_java.pl");

if($nostatus != 0)
{
	print "PERIODIC_HOLD: failed no trigger test\n";
}

if($willstatus != 0)
{
	print "PERIODIC_HOLD: failed trigger test\n";
}

if(($willstatus != 0) || ($nostatus != 0))
{
	die "Failed one PERIODIC_HOLD test\n";
}


