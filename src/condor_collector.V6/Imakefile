NAME = condor_collector
all_target(collector_engine.a $(NAME))

CFLAGS = $(STD_C_FLAGS)
C_PLUS_FLAGS = $(STD_C_PLUS_FLAGS)
LIB = collector_engine.a $(DAEMONCORE_LIB) $(STD_LIBS)

ENGINE_OBJ = hashkey.o collector_engine.o
OBJ = collector_main.o collector.o view_server.o \
      collector_instantiate.o totals.o collector_stats.o forkwork.o

library_target(collector_engine.a, $(ENGINE_OBJ))

c_plus_target($(NAME),$(OBJ),$(LIB))
release_target($(NAME),sbin,EXECUTABLE_MODE)
strip_target($(NAME),sbin,EXECUTABLE_MODE)
static_target($(NAME),$(OBJ),$(LIB),$(STATIC_DIR)/sbin)
pure_c_plus_target($(NAME).pure,$(OBJ),$(LIB))
purecov_c_plus_target($(NAME).pc,$(OBJ),$(LIB))

IMPORT_LINKS = ../../config/import_links
import(../condor_status.V6,totals.o)

html:
