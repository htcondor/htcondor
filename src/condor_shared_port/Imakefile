NAME = condor_shared_port
all_target( $(NAME) )

CFLAGS = $(STD_C_FLAGS)
C_PLUS_FLAGS = $(STD_C_PLUS_FLAGS)
LIB = $(DAEMONCORE_LIB) $(STD_LIBS) $(CONFIGURE_GSOAP_LDFLAGS)

soapobjs(shared_port)

OBJ = shared_port_main.o $(SOAP_OBJS)

public_c_plus_target($(NAME),libexec,$(OBJ),$(LIB))

html:
release:: all
stripped:: all
static:: all
clean::

gen_soapfiles(shared_port,condorShared_port)

testbin:: all
