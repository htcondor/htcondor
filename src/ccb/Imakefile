
CFLAGS = $(STD_C_FLAGS) 
C_PLUS_FLAGS = $(STD_C_PLUS_FLAGS)
LIB = $(STD_LIBS)

OBJ = ccb_server.o ccb_client.o ccb_listener.o

all_target($(OBJ))

release:: all
stripped:: all
static:: all
testbin:: all
