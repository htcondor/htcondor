NAME = condor_courier
all_target($(NAME))

CFLAGS = $(STD_C_FLAGS)
C_PLUS_FLAGS = $(STD_C_PLUS_FLAGS)
LIB = $(DAEMONCORE_LIB) $(STD_LIBS)

OBJ = courier_main.o

c_plus_target($(NAME),sbin,$(OBJ),$(LIB))

html:
release:: all
stripped:: all
static:: all
clean::

testbin:: all
