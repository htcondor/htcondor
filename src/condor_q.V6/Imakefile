NAME = condor_q
all_target($(NAME))

ANALYSIS_FLAGS = -DWANT_CLASSAD_ANALYSIS

LDFLAGS = $(STD_LDFLAGS) $(CONFIGURE_PCRE_LDFLAGS)
C_PLUS_FLAGS = $(ANALYSIS_FLAGS) $(STD_C_PLUS_FLAGS) $(POSTGRESQL_CFLAGS) $(TT_INC) $(CONFIGURE_POSTGRESQL_CFLAGS) $(CONFIGURE_PCRE_CFLAGS)
LIB = $(STD_LIBS) $(POSTGRESQL_LIBS) $(TT_LIB) $(CONFIGURE_POSTGRESQL_LDFLAGS)

OBJ = queue.o

public_c_plus_target($(NAME),bin,$(OBJ),$(LIB))

html:

testbin:: all
