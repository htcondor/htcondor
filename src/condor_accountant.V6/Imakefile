all_target(libacct.a)

C_PLUS_FLAGS = $(STD_C_PLUS_FLAGS)
CFLAGS = $(STD_C_FLAGS)

soapobjs(accountant)

LIB_OBJ = Accountant.o $(SOAP_OBJS)

OBJ = main.o

library_target(libacct.a,$(LIB_OBJ))

release:: all
stripped:: all
static:: all
html:
testbin:: all

gen_soapfiles(accountant,condorAccountant)
