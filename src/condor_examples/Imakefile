FILES = README condor.generic condor_config.generic \
	condor_config.local.central.manager condor_config.root.generic \
	condor_config.submit.generic

all:

clean:

release:
	/bin/cp -f $(FILES) $(RELEASE_DIR)/etc/examples
