NAME = condor_master
all_target($(NAME))

CFLAGS = $(STD_C_FLAGS) 
C_PLUS_FLAGS =  $(STD_C_PLUS_FLAGS)
LIB = $(DAEMONCORE_LIB) $(STD_LIBS)

soapobjs(master)

OBJ = master.o daemon.o $(SOAP_OBJS)

public_c_plus_target($(NAME),sbin,$(OBJ),$(LIB))

html:

gen_soapfiles(master,condorMaster)
