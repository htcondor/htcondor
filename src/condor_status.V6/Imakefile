NAME = condor_status
all_target($(NAME))

CFLAGS = $(STD_C_FLAGS) $(CONFIGURE_PCRE_CFLAGS)
C_PLUS_FLAGS = $(STD_C_PLUS_FLAGS) $(CONFIGURE_PCRE_CFLAGS)
LIB = $(STD_LIBS) $(CONFIGURE_PCRE_LDFLAGS)

OBJ = setflags.o status.o totals.o prettyPrint.o

public_c_plus_target($(NAME),bin,$(OBJ),$(LIB))

html:
testbin:: all
