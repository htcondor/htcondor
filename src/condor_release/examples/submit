#!/bin/sh

for i in io constructor env fstream loop registers \
	reader printer fortIO 
do
	if [ -x $i.remote ]; then 
		condor_submit $i.cmd
	fi
done

if [ -x sh_loop ]; then
	condor_submit sh_loop.cmd
fi