all_target(libstdunivio.a)

CFLAGS = $(STD_C_FLAGS) $(CONFIGURE_PCRE_CFLAGS)
C_PLUS_FLAGS = $(STD_C_PLUS_FLAGS) -DSKIP_AUTHENTICATION $(STD_UNIV_IO_FLAGS) -DHEADER_DES_LOCL_H $(CONFIGURE_PCRE_CFLAGS)

OBJ = std_univ_sock.o std_univ_buffers.o

library_target(libstdunivio.a,$(OBJ))

release:: all
stripped:: all
static:: all
testbin:: all

