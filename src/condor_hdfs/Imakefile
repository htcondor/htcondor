NAME = condor_hdfs
all_target($(NAME))

CFLAGS = $(STD_FLAGS)
C_PLUS_FLAGS = $(STD_C_PLUS_FLAGS)
LIB = $(DAEMONCORE_LIB) $(STD_LIBS)

OBJ = hadoop.o hadoop_main.o

public_c_plus_target($(NAME),sbin,$(OBJ),$(LIB))

html:
release:: all
stripped:: all
static:: all
clean::

release_recursive_copy_target($(HADOOP_DIR)/hdfs,libexec/hdfs)

testbin:: all
