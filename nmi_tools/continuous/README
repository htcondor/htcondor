To run a build on each commit, do the following:

1) In /home/condorauto, checkout a Git mirror clone using the command:
   git clone --mirror PATH-TO-CONDOR-REPO
2) In /home/condorauto, create a 'per-commit' directory
3) Copy submit and submit-per-commit-builds into that directory
4) condor_submit submit

Voila